import React from 'react';

const Headings = (props) => {

    // const headings = Object.keys(props.accountsData[0]).reduce((arr, key) => {
    //     if (key !== "isSelected") {
    //         arr = [...arr, <th key={key}>{key}</th>];
    //     }
    //     return arr;
    // }, []);

    return(
        <>
        
        </>
    )
}

export default Headings; 